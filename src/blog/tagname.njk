---
layout: page.njk
pagination:
  data: collections
  size: 1
  alias: tag
  filter:
    - all
    - now
eleventyComputed:
  title: "Tagged '{{ tag }}'"
permalink: /blog/tags/{{ tag }}/
---

{% set taglist = collections[ tag ] %}
<ul>
{% for post in taglist | reverse %}
  <li>
    <a href="{{ post.url }}">{{ post.data.title }}</a>
    ({% iso8601 post.data.date %})
  </li>
{% endfor %}
</ul>

<p>Go to <a href="/blog/tags">tag list</a>.</p>
