---
layout: page.njk
pagination:
  data: collections
  size: 1
  alias: tag
  filter:
    - rootLink
permalink: /blog/tags/{{ tag }}/
---

<h1>Tagged “{{ tag }}”</h1>

{% set taglist = collections[ tag ] %}
<ul>
{% for post in taglist | reverse %}
  <li>
    <a href="{{ post.url }}">{{ post.data.title }}</a>
    ({% iso8601 post.data.date %})
  </li>
{% endfor %}
</ul>
