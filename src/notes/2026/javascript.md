There is no longer any JavaScript served to you on this website! The back-to-top
button has been changed to be pure HTML and CSS; it was previously controlled
using a `<button>` element and an `onClick` function, but is now an anchor tag
using an `href` pointing to the topmost element of the page.
