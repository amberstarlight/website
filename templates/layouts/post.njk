---
layout: base.njk
---

{% set regExpCode = r/<pre class=(.|\n)*?<\/pre>/gm %}
{% set fixedContent = content | replace(regExpCode, "") | striptags %}
{% set wordCount = fixedContent | wordcount %}
{% include "components/breadcrumbs/breadcrumbs.njk" %}

<article class="post">
  <header>
    <h1 class="title">{{ title }}</h1>
    <h2>{{ subtitle }}</h2>
    <h3 class="dateline">{% niceDate page.date %} &bullet; {{ wordCount | numCommas }} words</h3>
    <ul class="tagslist">
    {% for tag in tags | filterTagList %}
      <li><a href="/blog/tags/{{ tag }}">#{{ tag }}</a></li>
    {% endfor %}
    </ul>
  </header>

  {{ content | safe }}
</article>
